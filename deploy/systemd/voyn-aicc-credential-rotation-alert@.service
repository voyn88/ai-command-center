[Unit]
Description=Alert for failed AICC credential rotation %I

[Service]
Type=oneshot
ExecStart=/usr/bin/logger -p daemon.err -t voyn-aicc-credential-rotation "AICC credential rotation failed for %I; inspect audit.jsonl and journalctl immediately"
