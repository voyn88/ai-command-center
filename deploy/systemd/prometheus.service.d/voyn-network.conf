# Prometheus binds to the control host's Tailscale address. An ordinary
# process exit during early boot must be retried after tailscaled is ready.
[Unit]
After=tailscaled.service network-online.target
Wants=tailscaled.service network-online.target

[Service]
Restart=on-failure
RestartSec=5s
