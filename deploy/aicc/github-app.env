# GitHub App the isolated fleet authenticates with (owner-registered, org voyn88).
# The private key is NOT here: /etc/voyn/secrets/aicc-github-app.pem, root 0600,
# placed by the owner. Tokens are minted by voyn-aicc-github-token.timer.
AICC_GITHUB_APP_ID=4884422
AICC_GITHUB_INSTALLATION_ID=160309192
