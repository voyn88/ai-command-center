# Canonical minimum lanes. Additional enabled/loaded template instances are
# discovered automatically; deployments can scale by adding instance names.
# Adding or removing a lane here also means updating
# deploy/aicc/voyn-worker-lanes.conf, credential rotation's own view of the
# same set.
1
2
